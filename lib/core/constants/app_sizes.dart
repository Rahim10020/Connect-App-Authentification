import 'package:flutter/material.dart';

//________________GLOBAL RADIUS___________________________

class AppRadius {
  static const small = Radius.circular(10);
  static const regular = Radius.circular(8);
  static const variant4 = Radius.circular(6);
  static const big = Radius.circular(50);
}
